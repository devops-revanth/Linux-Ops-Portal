The GitHub repository is the source of truth.

Review the latest code in the current workspace and continue from the existing implementation.

Do not rebuild or redesign any completed modules.

Implement only the following production-readiness improvements.

1. Authentication
- Add a login page.
- Protect all web UI routes with authentication.
- Use Flask-Login.
- Store users in the database.
- Passwords must be securely hashed.
- API token authentication for Ansible must remain separate and continue working.
- Redirect unauthenticated users to the login page.
- Add Logout functionality.

2. Inventory
- Implement bulk actions using the existing checkboxes.
- Support:
  - Bulk Delete
  - Bulk Change Environment
  - Bulk Change Location
- Add confirmation dialogs before destructive actions.

3. Patching
- Implement bulk actions using the existing checkboxes.
- Support:
  - Mark as Maintenance
  - Mark as Active
- Add a "Reboot Required" indicator.
- If reboot_required data is available from Ansible, display it.
- If not yet available, show "Unknown".

4. Settings
- Replace the Colour text column with visual Bootstrap badges or color swatches.
- Replace the hardcoded Replit API URL example with a configurable application base URL using configuration variables.
- Keep the API documentation section clean and production-ready.

5. User Experience
- Replace flash messages with Bootstrap toast notifications.
- Add loading indicators for long-running operations.
- Improve empty states where appropriate.

6. Error Pages
Create branded pages for:
- 401 Unauthorized
- 403 Forbidden
- 404 Not Found
- 500 Internal Server Error

Use the same dark theme as the rest of the portal.

7. Deployment Readiness
- Remove any remaining Replit-specific paths or URLs.
- Verify environment variables.
- Verify Docker deployment files.
- Verify docker-compose.yml.
- Verify health endpoint.
- Verify production configuration.

8. Validation
Test the complete application:

- Login
- Dashboard
- Inventory
- Server Details
- Patching
- Search
- Reports
- Settings
- Ansible API
- CSV/Excel exports
- Bulk actions

Fix any issues found during testing.

After completion:

- Commit all changes.
- Push everything to GitHub.
- Verify origin/main is fully synchronized.
- Provide:
  - Commit hash
  - Files created
  - Files modified
  - Summary of changes

Stop after all production-readiness improvements are complete.
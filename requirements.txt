# Core
Flask==3.1.0
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-WTF==1.2.1
Flask-Login==0.6.3

# Database
# psycopg2 (compiled from source) is required for production deployments.
# libpq-devel (RHEL) / libpq-dev (Debian) is installed by scripts/lib/deps.sh.
psycopg2==2.9.10

# Utilities
python-dotenv==1.0.1
WTForms==3.2.2

# Excel export
openpyxl==3.1.5

# LDAP / FreeIPA authentication
ldap3==2.9.1

# Encryption (bind password at rest)
cryptography==43.0.3

# VMware vSphere API
pyVmomi>=8.0.3.0.1

# Ansible control-node SSH connection
paramiko>=3.0.0

# Background scheduler (VMware scheduled sync)
APScheduler>=3.10.4

# Production WSGI server
gunicorn==23.0.0
